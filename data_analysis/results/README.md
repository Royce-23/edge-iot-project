# P3 results

`run_pipeline.py` writes the labelled feature dataset, run-level split, model,
predictions, metrics, plots and report here. The `demo/` result is generated
from simulated fan signals and must not be presented as hardware accuracy.
