# Dữ liệu giả dùng chung
features.sample.json tuân thủ schema v1; chỉ là ví dụ, không phải số đo thực nghiệm.
mock_api.py cần Python 3.9+, chỉ có devices/latest và phục vụ dashboard ở localhost:8000.
P4 thêm MQTT publisher sau khi chốt broker và thư viện; không có publisher thật trong starter.
