# Dashboard — P5
Chạy mock từ thư mục gốc theo README. Không mở index.html bằng file:// vì API cần HTTP.
api.js đã có getLatest(); realtime.js tải một lần và khi bấm nút.
Biểu đồ, alarm history, polling và stale indicator là phần P5 cần triển khai.
Khi backend thật có API cùng contract, dùng cùng origin hoặc cấu hình CORS phù hợp.
NDASHBOARD